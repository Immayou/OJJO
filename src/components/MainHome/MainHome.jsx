import { TopWrapper, BottomWrapper } from './MainHome.styled';
import { MainWrapper } from '../MainWrapper/MainWrapper';

export const MainHome = () => {
  return (
    <MainWrapper>
      <TopWrapper />
      <BottomWrapper />
    </MainWrapper>
  );
};
